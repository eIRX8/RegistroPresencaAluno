<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exemplo.registropresenca">

    <!-- Permissões -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar"
        android:usesCleartextTraffic="true">

        <!-- Tela de Login Biométrico (Launcher) -->
        <activity
            android:name=".ui.login.BiometricActivity"
            android:label="AppV3"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tela de Cadastro Inicial -->
        <activity
            android:name=".ui.cadastro.CadastroActivity"
            android:label="Cadastro do Aluno" />

        <!-- Tela de Registro de Presença -->
        <activity
            android:name=".ui.registro.RegistroActivity"
            android:label="Registro de Presença" />

        <!-- Tela de Sucesso -->
        <activity
            android:name=".ui.registro.SucessoActivity"
            android:label="Presença Registrada" />

    </application>

</manifest>